import React from 'react'
import Button from './Button';
const Layout = () => {
  return (
    <div>
<div>Layout</div>

    <Button />
    <Button/>

    <Button /> // new button

    </div>
    
  )
}

export default Layout